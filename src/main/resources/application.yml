spring:
  datasource:
    url: jdbc:mysql://localhost/avoindata?autoReconnect=true&rewriteBatchedStatements=true&cachePrepStmts=true&useTimezone=true&serverTimezone=UTC
    username: root
    password:
  jpa:
    properties:
      hibernate.dialect: 'org.hibernate.spatial.dialect.mysql.MySQLSpatialDialect'
server:
  port: 8081

logging:
  level:
    root: INFO
#    org.springframework.web.servlet.DispatcherServlet: DEBUG
#    org.hibernate.SQL: DEBUG
#    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

digitraffic.max-returned-trains: 250

graphql:
  servlet:
    mapping: /graphql
    enabled: true

graphiql:
  pageTitle: Local GraphiQL

cache:
  expiryInSeconds: 15
  maxCacheSize: 5
