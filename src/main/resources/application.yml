spring:
  datasource:
    url: jdbc:mysql://localhost/avoindata?autoReconnect=true&rewriteBatchedStatements=true&cachePrepStmts=true&useSSL=false
    username: root
    password:

server:
  port: 8081

logging:
  level:
    root: INFO

graphql:
  servlet:
    mapping: /graphql
    enabled: true

cache:
  expiryInSeconds: 15
  maxCacheSize: 5
