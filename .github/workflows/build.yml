name: Build
on:
  push:
  workflow_dispatch:

jobs:
  build:
    if: github.repository != 'tmfg/digitraffic-rail-graphql'
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup Java
        uses: actions/setup-java@de7274f081f381c8f8158605e0321c36c376e2e6 # v6.0.1
        with:
          java-version: 25
          distribution: corretto
          cache: maven
      - name: Compile
        run: mvn -f pom.xml install -e -U -DskipTests=true -Ddependency-check.skip=true
      - name: Checkout rail
        run: git clone https://github.com/tmfg/digitraffic-rail
      - name: Setup database
        working-directory: digitraffic-rail/dbrail
        run: |
          docker compose up -d
      - name: Get database IP
        run: echo "db_addr=$(docker inspect dbrail-db-1 -f '{{range.NetworkSettings.Networks}}{{.IPAddress}}{{end}}')" >> $GITHUB_ENV
      - name: Run tests
        run: mvn -f pom.xml test -Dspring.datasource.url=jdbc:aws-wrapper:mysql://${{ env.db_addr }}/avoindata_test
      - name: Publish test results
        uses: dorny/test-reporter@a43b3a5f7366b97d083190328d2c652e1a8b6aa2 # v3.0.0
        if: success() || failure()
        with:
          name: jUnit tests
          path: target/surefire-reports/*.xml
          reporter: java-junit
